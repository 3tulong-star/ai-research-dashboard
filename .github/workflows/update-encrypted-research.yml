name: Legacy encrypted research snapshot (manual only)

on:
  workflow_dispatch:
  # Scheduled execution is intentionally disabled. The local transactional
  # SCORE-0.2 pipeline is authoritative; this legacy workflow remains manual-only.

permissions:
  contents: write

concurrency:
  group: update-encrypted-research
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-data.txt

      - name: Install A-share universe collector
        run: python -m pip install -r requirements-data.txt

      - name: Install dependencies
        run: npm ci

      - name: Build active full-market universe
        run: python scripts/collect_full_market_universe.py --output /tmp/full-a-share-universe.json

      - name: Test historical valuation and drawdown calculations
        run: python tests/test_market_history.py

      - name: Generate plaintext research snapshot in temporary storage
        env:
          RESEARCH_OUTPUT: /tmp/static-workspace.json
          RESEARCH_UNIVERSE: /tmp/full-a-share-universe.json
        run: npm run research:run

      - name: Validate and encrypt research snapshot
        run: |
          jq -e '.generatedAt and (.discoveryCandidates | length > 0) and (.snapshots | length > 0) and (.decisions | length > 0) and (all(.snapshots[]; .data_complete == 1 and (.valuation_percentile | type == "number") and (.drawdown | type == "number") and .valuation_history_count >= 500 and .price_history_count >= 200))' /tmp/static-workspace.json >/dev/null
          node scripts/encrypt-research-snapshot.mjs /tmp/static-workspace.json data/research-snapshot.enc.json
          rm -f /tmp/static-workspace.json

      - name: Commit encrypted snapshot when changed
        env:
          TARGET_BRANCH: ${{ github.ref_name }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/research-snapshot.enc.json
          if git diff --cached --quiet; then
            echo "Encrypted snapshot did not change."
            exit 0
          fi
          git commit -m "chore(data): update encrypted research snapshot"
          git push origin "HEAD:${TARGET_BRANCH}"
